version: '2'
volumes:
    resin-data:
services:
  helloworld:
    image: resin/raspberrypi3-python:slim
    volumes:
      - 'resin-data:/data'
    command: |
      /bin/bash -c "while ; echo \"Sleeping\"; sleep 1000; done"
